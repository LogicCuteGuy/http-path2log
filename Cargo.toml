[package]
name = "http-path2log"
version = "0.1.1"
edition = "2021"
description = "HTTP server that routes log messages to files based on URL path"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
chrono = "0.4"
